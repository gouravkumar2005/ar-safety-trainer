# Copy to .env.android (used by `npm run build:android` / `apk:*`).
# The accounts server the Android app talks to. Must be https:// — the app
# itself runs on https://localhost, so a plain http:// address is blocked.
VITE_API_URL=https://your-server.example.in
